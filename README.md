# LightCodeEditor

# コンパイル方法
* PowerShellを開き、当フォルダ下まで移動します。

* make.ps1 を実行します。

```powershell
> ./make.ps1
```

* プログラムを実行するには以下のコマンドを実行します。

```powershell
> ./make.ps1 run
```

* コンパイルで生成したファイルを削除するには以下を実行します。

```powershell
> ./make.ps1 clean
```
